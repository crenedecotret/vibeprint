1. **Add Settings field** — `src/bin/studio/types.rs:206-225`
   Insert into `Settings` struct:
   ```rust
   pub monitor_icc_profile_override: Option<String>,
   ```
   Place after `input_slot_key` (line 224), matching other optional fields.

2. **Add AppState fields** — `src/bin/studio/types.rs:229-368`
   In `AppState` struct, add two fields near other modal flags (around line 330-340):
   ```rust
   pub monitor_icc_profile_override: Option<String>,   // persisted path
   pub show_monitor_icc_picker: bool,                  // modal flag
   ```
   Note: Keeping raw `Option<Vec<u8>>` in memory: derive loaded ICC bytes separately from the path.

3. **Load override at startup** — `src/bin/studio/app.rs:115-141`
   In `App::new()`, after loading `s = Settings::load()`:
   - If `s.monitor_icc_profile_override` is Some(path) and file exists, read bytes into `monitor_icc_profile`
   - If file does not exist, clear that setting (drop it)
   - Fallback to `monitor_icc::get_monitor_profile()` only if override not available
   - Pass resulting `Option<Vec<u8>>` to `AppState::new(param monitor_icc_profile)`

4. **Add UI menu entry** — `src/bin/studio/ui/left_panel.rs:56-60`
   Replace `add_enabled(false, ...)` with:
   ```rust
   if ui.button("Monitor ICC Profile…").clicked() {
       self.state.show_monitor_icc_picker = true;
   }
   ```
   Keep min_size styling similar to About button (140×22).

5. **Create or adapt modal** — `src/bin/studio/ui/modals.rs`
   **Option A (reuse existing `show_icc_picker`):**
   - Add `monitor_icc_picker_mode: bool` to `AppState`
   - In `show_icc_picker`, set `monitor_icc_picker_mode = true` if `state.show_monitor_icc_picker`
   - At selection (lines 593-614), set:
     - If `monitor_icc_picker_mode`: `state.monitor_icc_profile_override = Some(path.clone())`
     - Else: existing `output_icc` assignment
   - Reset flag on close.
   - Also "Browse for File…" branch (around line 630-650) needs the same branching.
   - Show helpful modal title: use `"Select Monitor ICC Profile"` when in monitor mode.

   **Option B (simple dedicated modal duplicate):**
   - Copy `show_icc_picker` into `show_monitor_icc_picker`
   - Replace every `self.state.output_icc` with `self.state.monitor_icc_profile_override`
   - Replace `show_icc_picker` flag with `show_monitor_icc_picker`
   - No branching; clearer separation.

   **Recommended:** Option B (simpler, only ~10 lines changed, no complex state machine).

6. **Persist override** — `src/bin/studio/main.rs:41-72` and `modals.rs:437-470`
   - In `on_exit()` callback (main.rs), add field:
     ```rust
     monitor_icc_profile_override: self.state.monitor_icc_profile_override.clone(),
     ```
   - In the mid-session ICC filter save (modals.rs:437-470), add same field. That existing save occurs when filter changes; also add at end of ICC picker after setting override (if using Option B, add in `show_monitor_icc_picker` after path selection).

7. **Preview refresh** — Already handled: both existing picker and modal branches call `self.mark_preview_dirty()` (see modals.rs:613). That triggers `rebuild_canvas_texture()` which reads `self.state.monitor_icc_profile`. Ensure after override is loaded at startup, `mark_preview_dirty()` is called once to refresh display (App::new already sets initial texture on first frame).

---

## Files to Modify (complete list)
- `src/bin/studio/types.rs` — Settings + AppState fields
- `src/bin/studio/app.rs` — startup loading override
- `src/bin/studio/ui/left_panel.rs` — menu button
- `src/bin/studio/ui/modals.rs` — monitor ICC picker modal (Option B: new function)
- `src/bin/studio/main.rs` — include override in on_exit Settings build
- `src/bin/studio/ui/modals.rs` (again) — include override in ICC-filter-save Settings build (lines 437-470)

---

## Test Checklist
- [ ] New "Monitor ICC Profile…" menu item is clickable
- [ ] Picker shows scanned profiles and Browse option
- [ ] Selecting a profile immediately updates preview
- [ ] Override persists across restarts
- [ ] If saved ICC file is deleted, app falls back to auto-detection (and resumes normal operation)
- [ ] Clear/reset flow works (use browse to pick a different file or delete the saved setting)
